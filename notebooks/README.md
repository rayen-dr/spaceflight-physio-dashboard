# Notebooks

Exploratory analysis, one notebook per phase (e.g. `01_data_cleaning.ipynb`, `02_eda.ipynb`, `03_statistical_analysis.ipynb`). Notebooks are for exploration and prototyping only — finalized logic gets moved into `src/` so it can be reused by the dashboard.
